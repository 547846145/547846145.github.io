> 
```
con1 : 从class文件到内存中的类，按先后顺序需要经过加载、链接以及从初始化三大不走。其中链接过程需要验证，而内存中的类没有经过初始化，同样不能使用。
con2 : java类分为基本类型和引用类型。基本类型是有java虚拟机预先定义好的。
       引用类型分为四种: 类、接口、数组类和泛型参数。泛型参数会在编译过程中被搽除，加载方式不同。
       数组类是由java虚拟机直接生成的，其他两种有对应的字节流。       
```
> 加载
```
con1 : 加载是指查找字节流，并且据此创建类的过程。对于类、和接口类需要借助类加载器完成字节流的查找。
con2 : 启动类加载器是由C++实现的，是最高级别的类加载器，没有对应java对象，为null
con3 : 除了启动类加载器，其他都是java.lang.ClassLoader的子类，有对应的java对象。
con4 : 
```